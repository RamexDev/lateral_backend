# Backend API base URL (no trailing slash).
VITE_API_BASE_URL=http://localhost:3000

# Enable the development login screen outside Telegram.
# When false, the Mini App shows a "dev login disabled" notice instead.
VITE_ENABLE_DEV_LOGIN=true
